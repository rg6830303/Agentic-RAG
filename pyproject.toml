[project]
name = "agentic-rag"
version = "0.1.0"
description = "Advanced Agentic RAG with a Vercel-compatible API entrypoint."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.117.1",
    "requests>=2.32.0",
]

[project.scripts]
app = "api.index:app"
