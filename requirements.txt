# Vercel serverless API dependencies.
#
# The full local Streamlit/RAG app uses requirements-local.txt. Keeping this
# file slim prevents Vercel from installing heavy local-only packages such as
# Streamlit, FAISS, RAGAS, datasets, and pyarrow during deployment.
fastapi>=0.117.1
requests>=2.32.0
